# HTTP sempre; HTTPS opcional quando existirem fullchain.pem/privkey.pem em docker/nginx/ssl/ na VPS.
server {
    listen 80 default_server;
    index index.php index.html;
    root /var/www/public;

    include /var/www/docker/nginx/laravel-php.inc;
}
